# Krish Portfolio (Vite + React + Tailwind)

## Commands
- `npm install`
- `npm run dev` to preview locally
- `npm run build` for production
- `npm run preview` to preview the build

## Resume file
Place your resume at: `public/Krish_Jakhar_Resume.pdf` (exact name).
Your component already references `/Krish_Jakhar_Resume.pdf`.

## Deploy on Vercel
- Import this repo in Vercel (Framework: Vite)
- Build Command: `npm run build`
- Output: `dist`